name: CI

on:
  push:
  pull_request:

jobs:
  build-test:
    # 24.04 ships the C++23 toolchains AeroEdge + QuarkCpp need (std::expected, deducing-this):
    # g++-14 and clang-18 from the default repos. ubuntu-latest's defaults are too old.
    runs-on: ubuntu-24.04
    strategy:
      fail-fast: false
      matrix:
        # clang-18 must be pointed at gcc-14's libstdc++ (which has <expected>); g++-14 uses its own.
        include:
          - { compiler: g++-14,     build_type: Release, sanitizer: "",                 stdlib: "" }
          - { compiler: g++-14,     build_type: Debug,   sanitizer: "address,undefined", stdlib: "" }
          - { compiler: g++-14,     build_type: Debug,   sanitizer: "thread",            stdlib: "" }
          - { compiler: clang++-18, build_type: Release, sanitizer: "",                 stdlib: "--gcc-install-dir=/usr/lib/gcc/x86_64-linux-gnu/14" }
          - { compiler: clang++-18, build_type: Debug,   sanitizer: "address,undefined", stdlib: "--gcc-install-dir=/usr/lib/gcc/x86_64-linux-gnu/14" }
          - { compiler: clang++-18, build_type: Debug,   sanitizer: "thread",            stdlib: "--gcc-install-dir=/usr/lib/gcc/x86_64-linux-gnu/14" }
    name: ${{ matrix.compiler }} ${{ matrix.sanitizer == '' && 'release' || matrix.sanitizer }}
    steps:
      - uses: actions/checkout@v4
        with:
          path: AeroEdge
      # AeroEdge expects QuarkCpp as a sibling checkout (../QuarkCpp), or set -DQUARK_DIR.
      - name: Checkout QuarkCpp
        uses: actions/checkout@v4
        with:
          repository: ${{ github.repository_owner }}/QuarkCpp
          path: QuarkCpp
      - name: Install toolchain
        # g++-14 provides libstdc++-14 (which clang-18 uses for <expected>); ninja for the -G Ninja build.
        run: sudo apt-get update && sudo apt-get install -y cmake ninja-build g++-14 clang-18
      - name: Configure
        working-directory: AeroEdge
        run: >
          cmake -S . -B build -G Ninja
          -DCMAKE_CXX_COMPILER=${{ matrix.compiler }}
          -DCMAKE_CXX_FLAGS="${{ matrix.stdlib }}"
          -DCMAKE_EXE_LINKER_FLAGS="${{ matrix.stdlib }}"
          -DCMAKE_BUILD_TYPE=${{ matrix.build_type }}
          -DAEROEDGE_SANITIZER="${{ matrix.sanitizer }}"
          -DQUARK_DIR=${{ github.workspace }}/QuarkCpp
      - name: Build
        working-directory: AeroEdge
        run: cmake --build build -j
      - name: Test
        working-directory: AeroEdge
        run: ctest --test-dir build --output-on-failure
